{
  "name": "@raydeck/ddb-manager",
  "version": "2.1.3",
  "description": "Base class for managing DynamoDB model",
  "main": "lib/index.js",
  "types": "lib/types",
  "repository": "https://github.com/rhdeck/ddb-manager",
  "author": "Ray Deck",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && typedoc --readme none --mode file --excludeNotExported --includeVersion && git commit docs -m \"docs: updating docs\" && git push",
    "watch": "livelink watch",
    "prepublishOnly": "yarn build"
  },
  "devDependencies": {
    "@raydeck/livelink": "^1.5.2",
    "@types/node": "^14.0.22",
    "eslint": "^7.0.0",
    "typedoc": "^0.17.8",
    "typedoc-plugin-markdown": "^2.3.1",
    "typescript": "^3.9.6"
  },
  "dependencies": {
    "aws-sdk": "^2.610.0"
  },
  "files": [
    "src",
    "lib"
  ],
  "liveLink": {
    "ignoreMasks": [
      "package.tgz"
    ]
  }
}
