{
  "name": "@raydeck/ddb-manager",
  "version": "2.0.5",
  "description": "Base class for managing DynamoDB model",
  "main": "lib/index.js",
  "types": "lib/types",
  "repository": "https://github.com/rhdeck/ddb-manager",
  "author": "Ray Deck",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && typedoc && yarn pack -f package.tgz",
    "watch": "livelink watch",
    "prepublishOnly": "yarn build"
  },
  "devDependencies": {
    "@types/node": "^14.0.22",
    "eslint": "^7.0.0",
    "typedoc": "^0.17.8",
    "typescript": "^3.9.6"
  },
  "dependencies": {
    "@raydeck/livelink": "^1.5.2",
    "aws-sdk": "^2.610.0",
    "typedoc-plugin-markdown": "^2.3.1"
  },
  "files": [
    "src",
    "lib"
  ],
  "liveLink": {
    "ignoreMasks": [
      "package.tgz"
    ]
  }
}
